package engine.genericAI;

import engine.Grid;
import objects.PuzzleElement;

public interface Brain {
//    void bestMove(Grid grid, PuzzleElement.PartData piece, int limitHeight, Brain.Move move);
}
